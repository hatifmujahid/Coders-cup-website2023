import { describe, it, expect } from "vitest";
import AboutPage from "./aboutPage";

describe("AboutPage", () => {
    it("should render", () => {
        expect(AboutPage).toBeDefined();
    });
}
);
